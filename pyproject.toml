[project]
name = "cricgyan"
version = "0.1.0"
description = "IPL Intelligence Engine — ML pipeline and CricGyaan API"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = []

[tool.uv]
package = false
