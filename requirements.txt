# Vercel serverless: lite inference only (numpy + lightgbm). ~150–250 MB installed.
# Training / Streamlit / local API: use requirements-ui.txt
fastapi>=0.109.0
mangum>=0.17.0
numpy>=1.24.0,<2.3
lightgbm>=4.0.0,<4.6
