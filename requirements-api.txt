# Minimal deps for backend/ + models inference only (Vercel / Lambda ~500 MB limit).
# Full stack: pip install -r requirements.txt
fastapi>=0.109.0
mangum>=0.17.0
numpy>=1.24.0,<2.3
pandas>=2.0.0,<2.3
scipy>=1.11.0,<1.15
scikit-learn>=1.3.0,<1.6
lightgbm>=4.0.0,<4.6
