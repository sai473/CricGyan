[project]
name = "cricgyan"
version = "0.1.0"
description = "IPL Intelligence Engine — ML pipeline and CricGyaan API"
readme = "README.md"
# Align with .python-version and runtime.txt (Python 3.11).
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
